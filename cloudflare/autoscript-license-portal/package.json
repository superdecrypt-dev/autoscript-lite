{
  "name": "autoscript-license-portal",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker + Pages portal for Autoscript VPS IP licensing",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "dev:worker": "wrangler dev",
    "dev:pages": "npm run build:pages && wrangler pages dev dist --compatibility-date=2026-03-31 --port 8788",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "npm run build:pages && wrangler pages deploy dist",
    "d1:migrate:local": "wrangler d1 migrations apply LICENSE_DB --local",
    "d1:migrate:remote": "wrangler d1 migrations apply LICENSE_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.9.0"
  }
}
