# Skeleton HAProxy config for autoscript edge provider.
# Not active yet.
#
# Design reference:
# /root/project/autoscript/EDGE_PROVIDER_DESIGN.md
#
# Planned responsibilities:
# - bind public :80 and :443
# - classify HTTP/plaintext vs TLS on :80
# - classify HTTP-over-TLS vs SSH classic on :443
# - forward HTTP traffic to nginx internal backend
# - forward non-HTTP TLS traffic to SSH classic backend
