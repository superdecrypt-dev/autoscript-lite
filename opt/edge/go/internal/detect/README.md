# internal/detect

Placeholder untuk logic deteksi traffic awal.

Ruang lingkup yang direncanakan:

- HTTP plaintext vs TLS ClientHello pada `:80`
- HTTP-over-TLS vs non-HTTP setelah TLS terminate pada `:443`
- timeout deteksi awal yang aman untuk workflow injector
